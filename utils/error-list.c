struct ErrorList {
    
};

// Insertion 
void insertToErrorList(ErrorList* list) {
     
}

// Print 
void printList(ErrorList* list) {
    ErrorList* tmp = *list; 
    while(tmp->next != null) {
        printf()
    }
    
}
